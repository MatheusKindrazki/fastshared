---
/**
 * BrandLockup — app icon + product name.
 * The landing rebrand uses the shipped app icon as the primary mark so the
 * website, README, and TestFlight builds all point at the same identity.
 */

interface Props {
  markSize?: number;
  textSize?: number;
  class?: string;
  ariaLabel?: string;
}

const {
  markSize = 22,
  textSize = 15,
  class: className = '',
  ariaLabel = 'FastShared',
} = Astro.props;
---
<span
  class={`brand-lockup inline-flex items-center ${className}`}
  style={`gap:${Math.max(8, Math.round(markSize * 0.36))}px;`}
  aria-label={ariaLabel}
>
  <img
    src="/brand/appicon-1024.png"
    alt=""
    width={markSize}
    height={markSize}
    class="brand-lockup__icon"
    style={`width:${markSize}px;height:${markSize}px;`}
    decoding="async"
    loading="lazy"
    aria-hidden="true"
  />
  <span
    class="font-display font-bold"
    style={`font-size:${textSize}px;letter-spacing:0;line-height:1;`}
  >
    FastShared
  </span>
</span>

<style>
  .brand-lockup__icon {
    display: block;
    border-radius: 22%;
    box-shadow: 0 8px 18px -12px rgba(15, 12, 34, 0.45);
  }
</style>
